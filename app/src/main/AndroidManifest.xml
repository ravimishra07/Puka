<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lukastore"
        tools:targetApi="31">
        <activity
            android:name=".BaseActivity"
            android:exported="false" />
        <activity
            android:name=".LogInSignUpSection.Activity.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".LogInSignUpSection.Activity.ForgotActivity"
            android:exported="false" />
        <activity
            android:name=".LogInSignUpSection.Activity.WalkthroughActivity"
            android:exported="false" />
        <activity
            android:name=".LogInSignUpSection.Activity.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".LogInSignUpSection.Activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LogInSignUpSection.Activity.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Lukastore" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>